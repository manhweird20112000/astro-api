export enum Status {
  inactive,
  active,
  approval,
  temporaryLock,
  block,
}

export enum Roles {
  SuperAdmin,
  Admin,
  Normal,
}

export enum PermissionAction {
  view,
  created,
  update,
  delete,
}

export enum Gender {
  female,
  male,
  other,
}
